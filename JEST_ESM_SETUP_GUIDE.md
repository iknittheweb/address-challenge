// Step-by-step Jest ESM reinitialization guide // 1. Install latest Jest and Babel dependencies: // npm install --save-dev jest@latest babel-jest @babel/core @babel/preset-env jest-environment-jsdom // 2. Create babel.config.cjs with: // module.exports = { presets: [['@babel/preset-env', { targets: { node: 'current' } }]] }; // 3. Create jest.config.cjs with: // module.exports = { // testEnvironment: 'jsdom', // transform: { '^.+\\.js$': 'babel-jest' }, // transformIgnorePatterns: ['/node_modules/(?!parse5|jsdom|@exodus/bytes)'], // setupFiles: ['<rootDir>/jest.setup.js'], // }; // 4. Use only .js files for tests, with ESM (import/export) syntax. // 5. Add scripts to package.json: // "test": "jest --config jest.config.cjs" // 6. Run tests with: npm test // // You can now re-add your test files and setup as needed.
